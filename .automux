version = 1
session_id = "parity-nas"

window "Editor" {
    exec = "vim"
    focus = true
}

window "Shell" {
    split {
        dir = "web/"
        exec = "npm run dev"
    }
    split {
        vertical = true
        exec = "air"
    }
}
